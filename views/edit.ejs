<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD Application </title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full h-full bg-zinc-900 p-10 text-white">
    <div class="nav mb-10">
      <a href="/read" class="text-blue-500">Read Users</a>
    </div>
    <h1 class="text-3xl tracking-tighter mb-10 ">UPDATE USER  😎</h1>
    <form action="/update/<%= user._id %>" method="post">
      <input type="text" placeholder="Name" class="px-5 py-2 bg-transparent border-2 border-zinc-200 rounded-lg outline-none" name="name" value="<%= user.name %>">
      <input type="text" placeholder="Email" class="px-5 py-2 bg-transparent border-2 border-zinc-200 rounded-lg outline-none" name="email" value="<%= user.email %>" >
      <input type="text" placeholder="Image Url" class="px-5 py-2 bg-transparent border-2 border-zinc-200 rounded-lg outline-none" name="image" value=" <%= user.image %>" >
      <input type="submit" value="Update User" class="px-5 py-2 bg-yellow-500 rounded-lg ml-3 text-black">
    </form>
  </div>
</body>
</html>